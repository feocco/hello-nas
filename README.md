# hello-nas

A disposable hello-world container for verifying the homelab NAS deploy path.

## Local

```bash
python3 -m unittest
SERVICE_PORT=8080 python3 server.py
```

Then open `http://localhost:8080`.

