interface:
  display_name: "Homelab Docker Deploy"
  short_description: "Deploy Docker services to a homelab NAS"
  default_prompt: "Use $homelab-docker-deploy to add this Dockerized service to my homelab deploy workflow."

